import ReactDOM from 'react-dom';
import Content from './Content';

const AppPaginaTributo = () => {
    return(
        <Content />
    );
}

ReactDOM.render(<AppPaginaTributo />, document.querySelector("#root"));